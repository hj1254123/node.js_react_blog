import React, { memo } from 'react'
import Sidebar from '../../components/Sidebar'

const TagsPage = memo(() => {
  return (
    <div>
      TagsPage
      <Sidebar />

    </div>
  )
})

export default TagsPage