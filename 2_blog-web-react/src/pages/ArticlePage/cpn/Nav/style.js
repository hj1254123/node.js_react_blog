import styled from 'styled-components';

export const NavWrapper = styled.nav`
  display: flex;
  justify-content: space-between;
  margin-top: 36px;
  > div {
    flex: 1;
  }
  .prev {
  } 
  .next {
    text-align: right;
  }
`
